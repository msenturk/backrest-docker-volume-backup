version: 2

env:
  - BACKREST_BUILD_VERSION={{.Version}}

before:
  hooks:
    - go mod tidy
    - pnpm --prefix webui install
    - sh -c "GOOS=linux BACKREST_BUILD_VERSION={{.Version}} go generate ./..."
    - sh -c "GOOS=windows BACKREST_BUILD_VERSION={{.Version}} go generate ./..."

builds:
  - id: linux
    main: ./cmd/backrest
    binary: backrest
    env:
      - CGO_ENABLED=0
    tags:
      - tray
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - arm
      - "386"
    goarm:
      - 6
      - 7
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}
  - id: docker-entrypoint
    main: ./cmd/docker-entrypoint
    binary: docker-entrypoint
    env:
      - CGO_ENABLED=0
    goos:
      - linux
    goarch:
      - amd64
      - arm64
      - arm
      - "386"
    goarm:
      - 6
      - 7
    ldflags:
      - -s -w
  - id: windows
    main: ./cmd/backrest
    binary: backrest
    env:
      - CGO_ENABLED=1
    tags:
      - tray
    goos:
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}} -H windowsgui

archives:
  - id: default
    ids:
      - linux
      - windows
    formats:
      - tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        formats: zip
    files:
      - install.sh
      - uninstall.sh
      - LICENSE
      - README.md
      - CHANGELOG.md
    allow_different_binary_count: true

dockers_v2:
  - id: alpine
    images:
      - "msenturk/backrest-docker-volume-backup"
      - "ghcr.io/msenturk/backrest-docker-volume-backup"
    tags:
      - latest
      - latest-alpine
      - v{{ .Major }}
      - v{{ .Major }}.{{ .Minor }}
      - "{{ .Tag }}"
      - "{{ .Tag }}-alpine"
    dockerfile: Dockerfile.alpine
    flags:
      - "--pull"
      # provenance attestations break the multi-arch manifest for armv6/armv7/386
      - "--provenance=false"
    ids:
      - linux
      - docker-entrypoint
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/arm/v6
      - linux/arm/v7
      - linux/386

  - id: scratch
    images:
      - msenturk/backrest-docker-volume-backup
      - ghcr.io/msenturk/backrest-docker-volume-backup
    tags:
      - scratch
      - v{{ .Major }}-scratch
      - v{{ .Major }}.{{ .Minor }}-scratch
      - "{{ .Tag }}-scratch"
    dockerfile: Dockerfile.scratch
    flags:
      - "--pull"
      # provenance attestations break the multi-arch manifest for armv6/armv7/386
      - "--provenance=false"
    ids:
      - linux
      - docker-entrypoint
    platforms:
      - linux/amd64
      - linux/arm64
      - linux/arm/v6
      - linux/arm/v7
      - linux/386

brews:
  - name: backrest
    ids:
      - default
    homepage: https://github.com/msenturk/backrest-docker-volume-backup
    description: "Backrest is a web UI and orchestrator for restic backup."

    url_template: "https://github.com/msenturk/backrest-docker-volume-backup/releases/download/{{ .Tag }}/{{ .ArtifactName }}"

    commit_author:
      name: goreleaserbot
      email: bot@goreleaser.com

    repository:
      owner: msenturk
      name: homebrew-backrest-tap
      branch: main
      token: "{{ .Env.HOMEBREW_GITHUB_TOKEN }}"

    service: |
      run opt_bin/"backrest"
      error_log_path var/"log/backrest.log"
      log_path var/"log/backrest.log"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"

release:
  github:
    owner: msenturk
    name: backrest-docker-volume-backup
